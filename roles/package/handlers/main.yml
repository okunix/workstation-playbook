---
- name: Creating facts.d directory
  ansible.builtin.file:
    state: directory
    path: /etc/ansible/facts.d
    owner: root
    mode: "0750"
  listen: Save state

- name: Saving current state
  ansible.builtin.copy:
    dest: /etc/ansible/facts.d/packages.fact
    content: "{{ package_install | to_json }}"
    owner: root
    mode: "0640"
  listen: Save state
